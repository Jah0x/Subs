# === App ===
HOSTNAME=zerologsvpn.com
XRAY_PORT=443
DEFAULT_SNI=www.twitch.tv
DEFAULT_PBK=CHANGE_ME_PBK
DEFAULT_SID=09c88c34c2a5fdb7
DEFAULT_FP=chrome
HMAC_SECRET=change_me_super_secret_key
LINK_TTL=0

# === Redis ===
REDIS_URL=redis://redis:6379/0
REDIS_CHANNEL=uid_events

# === Postgres ===
POSTGRES_HOST=postgres
POSTGRES_PORT=5432
POSTGRES_DB=subserver
POSTGRES_USER=subserver
POSTGRES_PASSWORD=subserver_pass
# Optional override (if set, used directly)
# DB_DSN=postgresql+asyncpg://subserver:subserver_pass@postgres:5432/subserver
